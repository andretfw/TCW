import {
  createStandardPageMetadata,
  StandardPageLayout,
} from '@/lib/standard-page-layout';

export const generateMetadata = createStandardPageMetadata('peerPolicy');
export default StandardPageLayout;
