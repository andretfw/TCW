import {
  createStandardPageMetadata,
  StandardPageLayout,
} from '@/lib/standard-page-layout';

export const generateMetadata = createStandardPageMetadata('privacy');
export default StandardPageLayout;
