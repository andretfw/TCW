import {
  createStandardPageMetadata,
  StandardPageLayout,
} from '@/lib/standard-page-layout';

export const generateMetadata = createStandardPageMetadata(
  'understandingDiagnosis',
);
export default StandardPageLayout;
