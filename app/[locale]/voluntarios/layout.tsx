import {
  createStandardPageMetadata,
  StandardPageLayout,
} from '@/lib/standard-page-layout';

export const generateMetadata = createStandardPageMetadata('volunteers');
export default StandardPageLayout;
